# dependencies
node_modules

# next.js build output
.next
out

# env files (secrets — never commit these)
.env
.env.local
.env.*.local

# misc
.DS_Store
*.tsbuildinfo
npm-debug.log*

# local database — content lives in prisma/seed-data.json, not in a binary blob
prisma/*.db
prisma/*.db-journal
